The persistent assistant floating action button — bottom-right navy circle, on every route.

```jsx
<Fab onClick={openAssistant} />
```

Rendered automatically by `AppShell`; use directly only outside the shell.
