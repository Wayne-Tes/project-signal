The top-of-page header: Poppins title + muted subtitle + optional action. Open every route with one.

```jsx
<PageHeader
  title="Signal inbox"
  subtitle="Severity-graded daily digests per account — every line cites its source."
  action={<Button>Pre-create user</Button>}
/>
```
