{
  "name": "report-worker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/report-worker/src",
  "projectType": "application",
  "tags": ["type:app", "scope:report-worker"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "yarn build", "cwd": "apps/report-worker" },
      "cache": true
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": { "command": "yarn dev", "cwd": "apps/report-worker" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "yarn typecheck", "cwd": "apps/report-worker" },
      "cache": true
    }
  }
}
