{
  "name": "@project-signal/ingestion",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch --env-file-if-exists=../../.env src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@project-signal/config": "workspace:*",
    "@project-signal/db": "workspace:*",
    "@project-signal/messaging": "workspace:*",
    "@project-signal/scoring": "workspace:*",
    "@project-signal/shared-types": "workspace:*",
    "@project-signal/source-adapters": "workspace:*",
    "@project-signal/storage": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
